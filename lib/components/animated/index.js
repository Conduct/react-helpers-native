import { animated } from "react-spring/native";
import { View, Text, TouchableOpacity, TextInput, ImageBackground, } from "react-native";
var AnimatedText = animated(Text);
var AnimatedView = animated(View);
var AnimatedTouchableOpacity = animated(TouchableOpacity);
var AnimatedTextInput = animated(TextInput);
var AnimatedImageBackground = animated(ImageBackground);
export { AnimatedView, AnimatedText, AnimatedTouchableOpacity, AnimatedTextInput, AnimatedImageBackground, };
